# 时间管理页防误操作与降廉价感优化（Design）

## 目标

- 创建待办流程更稳，减少误操作。
- 页面视觉更协调，去掉冗余与廉价感。
- 基于现有结构做轻量改造，不改数据模型。

## 本次实现范围（P0/P1）

- 时间轴删除改为“确认 + 5秒撤销”。
- 顶部按钮按“视图组 / 工具组”分离，降低混杂感。
- 待办创建按钮支持禁用态，避免空标题误提交。
- 待办池空状态改为引导卡片，压缩冗余空白。
- 统一关键文案（快速创建 -> 新建待办）。

## 不在本次范围

- 全量组件库重构。
- 复杂菜单系统（如完整三点菜单）。
- 新后端接口或持久化协议变更。

